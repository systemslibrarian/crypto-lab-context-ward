/**
 * Accepted non-text-contrast items.
 *
 * Every entry needs a reason. An empty baseline is the goal, and re-recording
 * this file to make a run go green is exactly the thing the gate exists to stop.
 */
export const NONTEXT_BASELINE: { selector: string; why: string }[] = []
